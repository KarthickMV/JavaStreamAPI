package com.Karthick.stream.stub;

import com.Karthick.stream.basics.ReferencingStream;
import com.Karthick.stream.basics.StreamInputTypes;

public class JavaExecutor {
	
	public static void main(String[] args) {
		
		
		/*
		 * StreamInputTypes inputTypes = new StreamInputTypes();
		 * inputTypes.inputTypes();
		 */
		
		ReferencingStream ref = new ReferencingStream();
		ref.referenceStream();
	}

}
